export interface TableProps {
    children?: React.ReactNode;
    disabled?: boolean;
    backgroundColor?: string;
    width?: string;
    striped?: boolean;
    hoverable?: boolean;
    responsive?: boolean;
  }
  